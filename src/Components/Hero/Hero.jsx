import "./heroStyles.css";

const Hero = () => {
    return (
        // <!-- Hero Section -->
        <section class="hero">
            <h1>Fast, Reliable Faxing for Your Business</h1>
            <p>Send and receive secure faxes anywhere, anytime.</p>
            <button class="cta-button">Get Started</button>
        </section>
    );
};

export default Hero;
